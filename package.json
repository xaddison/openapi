{
  "name": "@zeit/openapi-project",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "scripts": {
    "lerna": "lerna",
    "build": "yarn prepublish",
    "clean": "lerna clean && lerna run clean",
    "prepublish": "lerna run prepublish",
    "publish-stable": "yarn lerna publish --force-publish",
    "prettier": "prettier --write \"**/*.{js,jsx,json,ts,tsx,md,mdx,css,html,yml,yaml}\""
  },
  "devDependencies": {
    "lerna": "4.0.0"
  },
  "version": "0.0.3"
}
